To launch weather-app, firstly start docker, then click on cadence-start.bat and then run weather app application.

Get weather forecast URL: http://localhost:8080/weather?location='your_city'
Example: http://localhost:8080/weather?location=minsk

Data stored in WEATHER.db


